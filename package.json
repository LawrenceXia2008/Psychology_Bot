{
  "scripts": {
    "dev": "direct dev",
    "build": "direct build",
    "server": "direct server",
    "upload": "direct build && git save && git quickcm && git upload",
    "flow": "flow"
  },
  "name": "psychology_bot",
  "version": "1.0.0",
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "direct": "^0.1.1",
    "simple-direct": "^1.0.8"
  }
}
