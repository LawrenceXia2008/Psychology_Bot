{
  "scripts": {
    "dev": "direct dev",
    "build": "direct build",
    "server": "direct server",
    "upload": "direct build && git save && git quickcm && git upload",
    "flow": "flow"
  },
  "name": "psychology_bot",
  "version": "1.0.0",
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "ajv": "^6.4.0",
    "direct": "^0.1.1",
    "direct-core": "^1.0.6",
    "simple-direct": "^1.1.2"
  }
}
